const commentsRouter = require('express').Router();

// const commentsController = require('../../../../../Controller/api/user/social/comments');

// commentsRouter.get('/', commentsController.getComments); // gets the current user's social comments
// commentsRouter.get('/edit/:id', commentsController.editComments); // gets the current user's social comments
// commentsRouter.post('/update/:id', commentsController.updateComments); // gets the current user's social comments
// commentsRouter.delete('/delete/:id', commentsController.deleteComments); // gets the current user's social comments

module.exports = commentsRouter;