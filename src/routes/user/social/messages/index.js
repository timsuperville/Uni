const messagesRouter = require('express').Router();

// const messagesController = require('../../../../../controllers/user/social/messages/index.js');

// messagesRouter.get('/', messagesController.getMessages); // gets the current user's social messages
// messagesRouter.get('/edit/:id', messagesController.editMessages); // gets the current user's social messages
// messagesRouter.post('/update/:id', messagesController.updateMessages); // gets the current user's social messages
// messagesRouter.delete('/delete/:id', messagesController.deleteMessages); // gets the current user's social messages

module.exports = messagesRouter;