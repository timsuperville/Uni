const goalsRouter = require('express').Router();

// const goalsController = require('../../../../../controllers/user/education/goals/index.js');

// goalsRouter.get('/', goalsController.getGoals); // gets the current user's education goals
// goalsRouter.get('/edit/:id', goalsController.editGoals); // gets the current user's education goals
// goalsRouter.post('/update/:id', goalsController.updateGoals); // gets the current user's education goals
// goalsRouter.delete('/delete/:id', goalsController.deleteGoals); // gets the current user's education goals

module.exports = goalsRouter;