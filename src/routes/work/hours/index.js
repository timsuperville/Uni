const hours = require('express').Router();
// const hoursController = require('../../../controllers/work/hours/index.js');

// hours.get('/', hoursController.getWorkHours);
// hours.post('/', hoursController.addWorkHour);
// hours.delete('/:id', hoursController.deleteWorkHour);

module.exports = hours;