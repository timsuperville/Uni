const homeRouter = require('express').Router();

// const homeController = require('../../../controllers/home/index.js');

// homeRouter.get('/', homeController.getHome); // gets the user's home
// homeRouter.post('/create', homeController.createHome); // creates a new home
// homeRouter.put('/:id', homeController.updateHome); // updates the user's home
// homeRouter.delete('/:id', homeController.deleteHome); // deletes the user's home

module.exports = homeRouter;