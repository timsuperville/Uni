const alarmsRouter = require('express').Router();

// const alarmsController = require('../../../../../controllers/home/security/alarms');

// alarmsRouter.get('/', alarmsController.getAlarms);
// alarmsRouter.post('/', alarmsController.createAlarm);
// alarmsRouter.put('/:id', alarmsController.updateAlarm);
// alarmsRouter.delete('/:id', alarmsController.deleteAlarm);

module.exports = alarmsRouter;