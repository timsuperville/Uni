const settingsRouter = require('express').Router();

// const settingsController = require('../../../../../controllers/home/settings');

// settingsRouter.get('/', settingsController.getSettings);
// settingsRouter.post('/', settingsController.createSetting);
// settingsRouter.put('/:id', settingsController.updateSetting);
// settingsRouter.delete('/:id', settingsController.deleteSetting);

module.exports = settingsRouter;