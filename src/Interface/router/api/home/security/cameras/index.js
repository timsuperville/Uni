const camerasRouter = require('express').Router();

// const camerasController = require('../../../../../controllers/home/security/cameras');

// camerasRouter.get('/', camerasController.getCameras);
// camerasRouter.post('/', camerasController.createCamera);
// camerasRouter.put('/:id', camerasController.updateCamera);
// camerasRouter.delete('/:id', camerasController.deleteCamera);

module.exports = camerasRouter;