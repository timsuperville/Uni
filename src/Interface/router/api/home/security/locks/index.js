const locksRouter = require('express').Router();

// const locksController = require('../../../../../controllers/home/security/locks');

// locksRouter.get('/', locksController.getLocks);
// locksRouter.post('/', locksController.createLock);
// locksRouter.put('/:id', locksController.updateLock);
// locksRouter.delete('/:id', locksController.deleteLock);

module.exports = locksRouter;