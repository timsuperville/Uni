const listsRouter = require('express').Router();

// const listsController = require('../../../../../controllers/home/lists');

// listsRouter.get('/', listsController.getLists);
// listsRouter.post('/', listsController.createList);
// listsRouter.put('/:id', listsController.updateList);
// listsRouter.delete('/:id', listsController.deleteList);

module.exports = listsRouter;