const pantryRouter = require('express').Router();

// const pantryController = require('../../../../../controllers/home/food/pantry');

// pantryRouter.get('/', pantryController.getPantryItems);
// pantryRouter.post('/', pantryController.createPantryItem);
// pantryRouter.put('/:id', pantryController.updatePantryItem);
// pantryRouter.delete('/:id', pantryController.deletePantryItem);

module.exports = pantryRouter;