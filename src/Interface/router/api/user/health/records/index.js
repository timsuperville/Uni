const recordsRouter = require('express').Router();

// const recordsController = require('../../../../../controllers/user/health/records');

// recordsRouter.get('/', recordsController.getRecords); // Get all records
// recordsRouter.post('/', recordsController.createRecord); // Create a new record
// recordsRouter.get('/:recordId', recordsController.getRecord); // Get a record by id
// recordsRouter.put('/:recordId', recordsController.updateRecord); // Update a record by id
// recordsRouter.delete('/:recordId', recordsController.deleteRecord); // Delete a record by id

module.exports = recordsRouter;