const reportsRouter = require('express').Router();

// const reportsController = require('../../../../../controllers/user/education/reports/index.js');

// reportsRouter.get('/', reportsController.getReports); // gets the current user's education reports
// reportsRouter.get('/edit/:id', reportsController.editReports); // gets the current user's education reports
// reportsRouter.post('/update/:id', reportsController.updateReports); // gets the current user's education reports
// reportsRouter.delete('/delete/:id', reportsController.deleteReports); // gets the current user's education reports

module.exports = reportsRouter;