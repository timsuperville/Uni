const historyRouter = require('express').Router();

// const historyController = require('../../../../../controllers/user/education/history/index.js');

// historyRouter.get('/', historyController.getHistory); // gets the current user's education history
// historyRouter.get('/edit/:id', historyController.editHistory); // gets the current user's education history
// historyRouter.post('/update/:id', historyController.updateHistory); // gets the current user's education history
// historyRouter.delete('/delete/:id', historyController.deleteHistory); // gets the current user's education history

module.exports = historyRouter;