const resourcesRouter = require("express").Router();

// const resourcesController = require("../../../../../controllers/user/education/resources/index.js");

// resourcesRouter.get("/", resourcesController.getResources); // gets the current user's education resources
// resourcesRouter.get("/edit/:id", resourcesController.editResources); // gets the current user's education resources
// resourcesRouter.post("/update/:id", resourcesController.updateResources); // gets the current user's education resources
// resourcesRouter.delete("/delete/:id", resourcesController.deleteResources); // gets the current user's education resources

module.exports = resourcesRouter;
