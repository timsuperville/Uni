const postsRouter = require('express').Router();

// const postsController = require('../../../../../controller/api/user/social/posts');

// postsRouter.get('/', postsController.getPosts); // gets the current user's social posts
// postsRouter.get('/edit/:id', postsController.editPosts); // gets the current user's social posts
// postsRouter.post('/update/:id', postsController.updatePosts); // gets the current user's social posts
// postsRouter.delete('/delete/:id', postsController.deletePosts); // gets the current user's social posts

module.exports = postsRouter;