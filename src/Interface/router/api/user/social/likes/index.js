const likesRouter = require('express').Router();

// const likesController = require('../../../../../Controller/api/user/social/likes');

// likesRouter.get('/', likesController.getLikes); // gets the current user's social likes
// likesRouter.get('/edit/:id', likesController.editLikes); // gets the current user's social likes
// likesRouter.post('/update/:id', likesController.updateLikes); // gets the current user's social likes
// likesRouter.delete('/delete/:id', likesController.deleteLikes); // gets the current user's social likes

module.exports = likesRouter;