const transactionsRouter = require('express').Router();

// const transactionsController = require('../../../../../controllers/user/finance/transactions');

// transactionsRouter.get('/', transactionsController.getTransactions); // Get all transactions
// transactionsRouter.post('/', transactionsController.createTransaction); // Create a new transaction
// transactionsRouter.get('/:transactionId', transactionsController.getTransaction); // Get a transaction by id
// transactionsRouter.put('/:transactionId', transactionsController.updateTransaction); // Update a transaction by id
// transactionsRouter.delete('/:transactionId', transactionsController.deleteTransaction); // Delete a transaction by id

module.exports = transactionsRouter;